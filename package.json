{
  "name": "tidy-001",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "schema:check": "jq '.response' api/clean_task_example.json > /tmp/clean_resp.json && ajv validate -s schema/clean_task.response.schema.json -c ajv-formats -d /tmp/clean_resp.json && jq '.response' api/prioritize_tasks_example.json > /tmp/prioritize_resp.json && ajv validate -s schema/prioritize.response.schema.json -c ajv-formats -d /tmp/prioritize_resp.json",
    "types:gen": "typescript-json-schema \"schema/**/*.json\" \"*\" --out types/generated.d.ts"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "date-fns": "^4.1.0",
    "date-fns-tz": "^3.2.0",
    "next": "14.2.35",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/react": "^18.2.74",
    "ajv-cli": "^5.0.0",
    "typescript": "^5.6.3",
    "typescript-json-schema": "^0.65.1"
  }
}
